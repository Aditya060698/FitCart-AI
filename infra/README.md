# Infrastructure

This folder is for local runtime and deployment-related assets.

Suggested contents:

- `docker-compose.yml`
- database initialization scripts
- deployment manifests
- reverse proxy configs
- CI/CD templates

Keep infrastructure concerns separate from app source code.
