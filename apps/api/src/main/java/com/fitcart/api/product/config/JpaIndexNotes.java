package com.fitcart.api.product.config;

// Placeholder package anchor for future Flyway or schema-level product indexing notes.
// Keep package if feature-specific configuration is needed later.
public final class JpaIndexNotes {
    private JpaIndexNotes() {
    }
}
