# Applications

This directory contains deployable applications.

- `web`: React frontend
- `api`: Spring Boot backend
- `ai`: Python FastAPI AI service

Keep shared code minimal at first. It is better to duplicate a tiny amount of code than to create premature coupling.
